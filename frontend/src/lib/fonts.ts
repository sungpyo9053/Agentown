// Loaded via a plain Google Fonts <link> in app/layout.tsx (see .font-block in globals.css)
// instead of next/font/google, because the Korean subset isn't available through
// next/font in this Next.js version's bundled font metadata.
export const blockFontClassName = "font-block";
