"use client";
import {FormEvent} from "react";import {useMutation} from "@tanstack/react-query";import {useRouter} from "next/navigation";import {AppShell} from "@/components/AppShell";import {api} from "@/lib/api";
export default function Page(){const router=useRouter();const create=useMutation({mutationFn:(body:unknown)=>api<{id:string}>("/harnesses",{method:"POST",body:JSON.stringify(body)}),onSuccess:data=>router.push(`/harnesses/${data.id}/edit`)});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);create.mutate({name:f.get("name"),description:f.get("description"),visibility:"PRIVATE"})}return <AppShell kicker="HARNESS DESIGNER" title="새 AI 팀 만들기"><form onSubmit={submit} className="mx-auto max-w-2xl space-y-5 rounded-3xl bg-white p-8 shadow-card"><label className="block font-bold">팀 이름<input name="name" required maxLength={100} placeholder="블로그 제작팀" className="mt-2 w-full rounded-xl border p-4" /></label><label className="block font-bold">팀 목표<textarea name="description" rows={4} placeholder="검색 의도에 맞는 글을 작성하고 사실을 검수한다." className="mt-2 w-full rounded-xl border p-4" /></label><button className="w-full rounded-xl bg-coral p-4 font-black text-white">팀 만들고 구성원 연결</button>{create.error&&<p className="text-red-600">{create.error.message}</p>}</form></AppShell>}
